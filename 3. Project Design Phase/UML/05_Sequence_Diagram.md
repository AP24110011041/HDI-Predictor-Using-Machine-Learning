# Sequence Diagram

## Sequence Overview
1. The user submits input values through the web form.
2. The Flask application receives the request.
3. The application passes the values to the prediction model.
4. The model returns the predicted HDI.
5. The application displays the result to the user.
